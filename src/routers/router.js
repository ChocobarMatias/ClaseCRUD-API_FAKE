// rutas para cada pagina que se va a utilizar

export const LOGIN ="/"  //ruta exportada por constante o constate habilitada para ser importada en otro componente
export const HOME ="/home"
export const CREAR = "/crear"
export const EDITAR = `/editar/:id` //:id es un parametro que se le pasa a la ruta, en este caso el id del elemento a editar
export const VER = "/ver/:id" //:id es un parametro que se le pasa a la ruta, en este caso el id del elemento a ver